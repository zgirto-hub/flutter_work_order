# Data Model: System Status Cards Redesign

No data model changes required for this feature.

This is a frontend-only UI layout redesign affecting three private
widgets within `system_status_screen.dart`. The `SystemStatus` and
`SystemStatusReport` models, the `SystemStatusService`, and all
backend endpoints remain unchanged.
